<UserControl x:Class="ComCon.Client.Modules.Login.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:com="clr-namespace:ComCon.Client.Base.Classes;assembly=ComCon.Client.Base"
             mc:Ignorable="d" 
             d:DesignHeight="270" d:DesignWidth="529">
    <StackPanel VerticalAlignment="Center">
        <AdornerDecorator>
            <TextBox Width="150" VerticalAlignment="Center" Margin="5" x:Name="NameBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                <com:WatermarkService.Watermark>
                    <TextBlock>Username</TextBlock>
                </com:WatermarkService.Watermark>
            </TextBox>
        </AdornerDecorator>

        <AdornerDecorator>
            <PasswordBox Width="150" VerticalAlignment="Center" Margin="5">
                <com:WatermarkService.Watermark>
                    <TextBlock>Passwort</TextBlock>
                </com:WatermarkService.Watermark>
            </PasswordBox>
        </AdornerDecorator>
        <Button Content="Login" Width="80" Margin="70,5,0,0" Command="{Binding LoginCommand}"></Button>
    </StackPanel>
</UserControl>
